<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/email_activity_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:openDrawer="start">

    <androidx.appcompat.widget.Toolbar
            android:id="@+id/email_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:layout_constraintTop_toTopOf="parent"
            android:theme="@style/ToolbarTheme"/>

    <ProgressBar
            android:id="@+id/config_loading"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:visibility="gone"/>

    <TextView
            android:id="@+id/inbox_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/inbox_string"
            android:textSize="16sp"
            android:textColor="@color/simple_text_selector"
            android:clickable="true"
            android:focusable="true"
            android:enabled="false"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/email_toolbar"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_marginStart="20dp"
            android:padding="5dp"/>

    <TextView
            android:id="@+id/sent_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/sent_string"
            android:textSize="16sp"
            android:textColor="@color/simple_text_selector"
            android:clickable="true"
            android:focusable="true"
            android:enabled="false"
            app:layout_constraintLeft_toRightOf="@id/inbox_text"
            app:layout_constraintTop_toBottomOf="@id/email_toolbar"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_marginStart="10dp"
            android:padding="5dp"/>

    <TextView
            android:id="@+id/write_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/write_string"
            android:textSize="16sp"
            android:textColor="@color/simple_text_selector"
            android:clickable="true"
            android:focusable="true"
            android:enabled="false"
            app:layout_constraintLeft_toRightOf="@id/sent_text"
            app:layout_constraintTop_toBottomOf="@id/email_toolbar"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_marginStart="10dp"
            android:padding="5dp"/>

    <TextView
            android:id="@+id/remove_config_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/remove_config_string"
            android:textSize="16sp"
            android:clickable="true"
            android:focusable="true"
            android:enabled="false"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/email_toolbar"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_marginEnd="10dp"
            android:padding="5dp"/>

    <androidx.viewpager.widget.ViewPager
            android:id="@+id/email_view_pager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="10dp"
            app:layout_constraintTop_toBottomOf="@id/inbox_text"
            app:layout_constraintBottom_toBottomOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>